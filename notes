#!/usr/bin/env python3
"""
Notes in the Terminal Application
Main executable script
"""

import sys
import os

# Add the parent directory to the path so we can import notes_app
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from notes_app.cli import main

if __name__ == '__main__':
    main()
